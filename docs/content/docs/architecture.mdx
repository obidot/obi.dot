---
title: Architecture
description: High-level architecture of the Obidot DEX aggregator on Polkadot Hub — system components, cross-chain data flow, actor roles, and the optional AI agent sub-feature.
---

## System Overview

Obidot is a modular DEX aggregator on Polkadot Hub where each component has a clearly defined responsibility. The core smart contract — `ObidotVault` — is an ERC-4626 vault that routes swaps through `SwapRouter` across on-hub DEX pools (via pluggable `IPoolAdapter` adapters), Polkadot parachains (XCM), and EVM chains (Hyperbridge ISMP). Satellite vaults on EVM chains sync state back to the hub, and Bifrost DeFi is integrated via parachain 2030. An optional AI agent sub-feature can sign and submit EIP-712 `StrategyIntent` structs for autonomous intent-based strategy execution. A Next.js dashboard provides a real-time operator interface.

```
                                    Polkadot Hub
                                    ┌─────────────────────────────────┐
Depositors ──► ObidotVault (ERC-4626) ──► XCM Precompile ──► Parachain DeFi
                  │                              ▲                │
                  ├── Policy Engine               │                │
                  ├── Oracle Slippage Guard        │                ▼
                  ├── EIP-712 Signature Verify     │         BifrostAdapter
                  ├── Remote Asset Accounting      │           │
                  └── CrossChainRouter ◄───────────┘           ▼
                         │                              Bifrost (2030)
AI Strategist ─── signs UniversalIntent ───────┘        SLP / DEX / Farm / SALP
                         │
Keeper ─────── reports outcome (P&L) ──────────┘
                         │
                    Hyperbridge (ISMP)
                    ┌────┴────────────────┐
                    ▼                     ▼
              Ethereum              Arbitrum/Optimism/Base
          ObidotVaultEVM            ObidotVaultEVM
           (Satellite)               (Satellite)

Off-Chain Layer
──────────────────────────────────────────────────────────
obi.index — GraphQL Indexer (port 4350)
  ├── Blockscout poller (15s) — fetches logs for all contracts with retry + reorg rewind/replay
  ├── Event handlers — vault, oracle, router, executor → PostgreSQL via Prisma
  ├── GraphQL API — analytics, price history, cross-chain pipeline queries + subscriptions
  ├── Operational endpoints — /health, /metrics, /analytics/materialized
  └── Optional AI agent sub-process
        ├── Perceive   — reads live vault state + oracle prices via RPC
        ├── Reason     — LLM decision (deploy / rebalance / hold)
        ├── Act        — signs + submits UniversalIntent / StrategyIntent on-chain
        └── Record     — Prisma writes indexed by strategyId

Next.js Dashboard (port 3010) — obidot/app
  ├── /           — Vault overview, deposits, withdrawals, recent activity
  ├── /swap       — Local swap, limit orders, cross-chain route planner
  ├── /crosschain — Chain topology, satellite table
  ├── /insights   — Indexed analytics, top routes, and price history
  └── /agent      — Streamed chat, proposal review, agent telemetry
```

## Actor Model

The system defines privileged roles using OpenZeppelin's `AccessControl`, plus unprivileged depositors and relayers.

### Depositors

Standard ERC-4626 users who deposit the underlying asset and receive vault shares in return. They interact exclusively through `deposit()`, `mint()`, `withdraw()`, and `redeem()`. Depositors have no special on-chain role — anyone with the underlying token can participate.

### Admin (`DEFAULT_ADMIN_ROLE`)

The governance layer. The admin configures all policy engine parameters and has emergency controls:

- Whitelist/delist parachains and protocols
- Set per-protocol exposure caps
- Configure daily loss thresholds
- Update the oracle address
- Adjust XCM weight limits
- Pause/unpause the vault
- Enable emergency withdrawal mode
- Manually adjust remote asset accounting

In production, this role should be held by a multisig or governance contract.

### AI Strategist / Solver (`STRATEGIST_ROLE` / `SOLVER_ROLE`)

An optional sub-feature. When enabled, an autonomous agent decides where to deploy capital. The strategist never calls the vault directly — instead, it signs an EIP-712 `StrategyIntent` struct off-chain. This signed intent is then submitted to the vault by a relayer.

The strategist's power is bounded by:

- Parachain whitelist (can only target approved chains)
- Protocol whitelist (can only target approved protocols)
- Exposure caps (cannot over-allocate to a single protocol)
- Oracle slippage bounds (expected return must be reasonable)
- Daily loss circuit breaker (vault auto-pauses on excessive losses)

### Keeper (`KEEPER_ROLE`)

An off-chain service that monitors cross-chain strategy outcomes and reports results back to the vault via `reportStrategyOutcome()`. The keeper provides:

- Whether the remote execution succeeded or failed
- The amount of assets returned
- The vault calculates P&L and updates accounting accordingly

If accumulated daily losses exceed the configured threshold, the circuit breaker triggers automatically.

### Relayer (Unprivileged)

Anyone can call `executeStrategy()` with a valid signed intent. This permissionless relaying model means the strategist doesn't need ETH/DOT for gas — any relayer can submit the transaction. The EIP-712 signature ensures only authorized strategists can originate valid intents.

## Contract Inheritance

`ObidotVault` inherits from four OpenZeppelin v5 base contracts:

| Base Contract     | Purpose                                                                |
| ----------------- | ---------------------------------------------------------------------- |
| `ERC4626`         | Standard yield-bearing vault interface (which itself inherits `ERC20`) |
| `AccessControl`   | Role-based access control for admin, strategist, and keeper            |
| `Pausable`        | Emergency pause mechanism for deposits and strategy execution          |
| `ReentrancyGuard` | Protection against reentrancy on all entry/exit points                 |

## Data Flow

### Deposit Flow

1. User calls `deposit(assets, receiver)` or `mint(shares, receiver)`
2. Vault checks: not paused, deposit cap not exceeded
3. Assets transferred from user to vault (increases idle balance)
4. Shares minted to receiver using ERC-4626 conversion math

### Strategy Execution Flow (AI Agent Sub-Feature)

1. AI strategist constructs a `UniversalIntent` with target destination (XCM parachain or EVM chain via Hyperbridge), amount, and encoded calldata
2. Strategist signs the intent using EIP-712 typed data
3. Relayer submits `executeIntent(intent, signature)` to the vault
4. Vault runs the full validation pipeline:
   - Deadline check
   - Basic validation (non-zero amount, asset match, slippage bounds)
   - EIP-712 signature recovery and `STRATEGIST_ROLE` verification
   - Nonce check and increment (replay protection)
   - Policy engine enforcement (whitelists, caps, circuit breaker)
   - Oracle slippage validation (Pyth price feed)
   - XCM weight estimation (pre-flight check with 10% safety margin)
5. Vault updates accounting: `totalRemoteAssets += amount`, `protocolExposure += amount`
6. Vault approves the XCM precompile and dispatches the cross-chain message

### Outcome Reporting Flow

1. Keeper observes the cross-chain result (success/failure, returned amount)
2. Keeper calls `reportStrategyOutcome(strategyId, success, returnedAmount)`
3. Vault calculates P&L: `pnl = returnedAmount - deployedAmount`
4. Remote asset accounting updated: `totalRemoteAssets -= deployedAmount`
5. Protocol exposure decremented
6. If loss detected: accumulated into daily loss tracker
7. If daily losses exceed threshold: vault auto-pauses + emergency mode enabled

### Withdrawal Flow

1. User calls `withdraw(assets, receiver, owner)` or `redeem(shares, receiver, owner)`
2. If paused and not in emergency mode: reverts
3. If emergency mode: `totalAssets()` returns only idle balance (ignores remote assets)
4. Shares burned, assets transferred to receiver

## Accounting Model

The vault tracks three categories of assets:

| Category             | Description                                                                                        |
| -------------------- | -------------------------------------------------------------------------------------------------- |
| **Idle Assets**      | Underlying tokens held directly in the vault contract (`IERC20(asset()).balanceOf(address(this))`) |
| **Remote Assets**    | Value deployed to parachains, tracked via `totalRemoteAssets` state variable                       |
| **Satellite Assets** | Value held in satellite vaults on EVM chains, tracked via `totalSatelliteAssets`                   |

```
totalAssets()       = idle + totalRemoteAssets              (hub, normal mode)
totalAssets()       = idle                                  (hub, emergency mode)
globalTotalAssets() = totalAssets() + totalSatelliteAssets  (cross-chain aggregate)
```

This multi-balance model means share prices reflect all capital across the system. The hub vault holds the authoritative share price. Satellite vaults receive synced global state via Hyperbridge ISMP messages and use `globalTotalAssets` / `globalTotalShares` for consistent pricing.

## Security Layers

Obidot employs defense-in-depth with multiple independent security mechanisms:

1. **EIP-712 Signatures** — Strategy intents are cryptographically signed with domain separation, preventing cross-contract and cross-chain replay
2. **Sequential Nonces** — Per-strategist nonce tracking prevents intent replay within the same contract
3. **Policy Engine** — On-chain whitelists and caps bound the strategist's operational scope
4. **Oracle Validation** — Real-time price checks ensure expected returns are economically reasonable
5. **XCM Weight Limits** — Pre-flight weight estimation prevents dispatching overweight messages
6. **Circuit Breaker** — Automatic pause when daily losses exceed the configured threshold
7. **Reentrancy Guard** — All external entry points are protected against reentrancy attacks
8. **Inflation Attack Mitigation** — Virtual share offset of `10^3` prevents share price manipulation at low liquidity

## File Organization

### Smart Contract Layer (`src/`)

```
src/
├── intent/
│   ├── IntentTypes.sol         # UniversalIntent struct + Destination enum
│   ├── IntentDomain.sol        # EIP-712 typed-data helpers + signature verification
│   └── IIntentSolver.sol       # AI → Vault handshake interface
├── executor/
│   ├── IExecutor.sol           # Single execute/quote interface (all chains)
│   ├── XCMExecutor.sol         # Native Polkadot XCM (parachains) via 0xA0000
│   └── HyperExecutor.sol       # Hyperbridge (ETH/Base/Arbitrum) via ISMP host
├── oracle/
│   ├── KeeperOracle.sol        # Keeper-updatable price feed (IAggregatorV3)
│   ├── IOracle.sol             # RedStone + Chainlink CCIP interface
│   └── SlippageGuard.sol       # 2% max-slippage validation library
├── asset/
│   ├── IAsset.sol              # AssetHub precompile interface
│   └── NativeAsset.sol         # USDC/DOT as ERC-20 proxy over AssetHub precompiles
├── vault/
│   ├── ObidotVault.sol         # Core ERC-4626 vault + IIntentSolver + SwapRouter integration
│   ├── ObidotVaultEVM.sol      # Satellite vault (EVM chains, syncs via Hyperbridge)
│   └── ERC4626Initializable.sol # Custom OZ ERC4626 with storage-based asset (for clone proxies)
├── router/
│   ├── ISwapRouter.sol         # DEX aggregator router interface
│   ├── IPoolAdapter.sol        # Universal pool adapter interface
│   ├── SwapRouter.sol          # Core router: single/multi-hop/split swaps, multicall
│   └── SwapQuoter.sol          # Read-only quoter: getBestQuote, getAllQuotes, quoteMultiHop
├── adapters/
│   ├── CrossChainRouter.sol    # Hub ISMP message router (dispatches + receives)
│   ├── BifrostAdapter.sol      # Bifrost SLP/SALP/DEX/Farming adapter (parachain 2030)
│   └── HyperbridgeAdapter.sol  # Abstract ISMP dispatch/onAccept/onTimeout base
├── interfaces/
│   ├── IIntentSolver.sol       # Canonical solver interface
│   ├── IAggregatorV3.sol       # Chainlink aggregator interface
│   ├── IXcm.sol                # XCM precompile (0xA0000) interface
│   ├── IIsmpHost.sol           # ISMP host interface
│   ├── IIsmpModule.sol         # ISMP module callback interface
│   ├── IOracleConsumer.sol     # Oracle consumer interface
│   └── IBifrost*.sol           # Bifrost protocol interfaces (SLP, SALP, DEX, Farming)
├── libraries/
│   ├── MultiLocation.sol       # XCM MultiLocation SCALE encoding
│   ├── BifrostCodec.sol        # Bifrost pallet call XCM V4 encoding
│   ├── CrossChainCodec.sol     # ISMP message encode/decode (7 message types)
│   ├── SafeTransfer.sol        # Universal asset-transfer helpers
│   └── XCMEncoder.sol          # MultiLocation factory functions
├── periphery/
│   ├── VaultFactory.sol        # Multi-strategy vault deploy (EIP-1167 clones — not deployable on PVM)
│   └── DemoIntent.sol          # Pre-signed intents (library, no state)
└── OracleRegistry.sol          # Multi-asset oracle registry with staleness checks
```

### Tests (`test/`)

```
test/
├── Abstractions.t.sol          # Intent / executor / oracle / asset unit tests
├── ObidotVault.t.sol           # Unit + fuzz + invariant tests (87 K, largest file)
├── OracleRegistry.t.sol        # Oracle registry tests
├── CrossChain.t.sol            # Satellite + adapter tests
├── CrossChainCodec.t.sol       # CrossChainCodec library tests
├── BifrostCodec.t.sol          # BifrostCodec library tests
├── KeeperOracle.t.sol          # KeeperOracle unit + fuzz tests
├── Integration.t.sol           # Full hub lifecycle (7 tests)
├── CrossChainLifecycle.t.sol   # Cross-chain lifecycle (9 tests)
├── SwapRouter.t.sol            # SwapRouter unit + fuzz tests (42 tests)
├── SwapQuoter.t.sol            # SwapQuoter unit tests (20 tests)
├── VaultSwapIntegration.t.sol  # End-to-end vault → router → pool tests (15 tests)
├── IntentSolver.t.sol          # Hyperbridge intent execution tests (16 tests)
└── PoolAdapters.t.sol          # UniswapV2PoolAdapter + MockBridgeAdapter tests (46 tests)
```

### Deployment Scripts (`script/`)

```
script/
├── Deploy.s.sol                # Production hub deploy + DeployWithSetup
├── DeployTestnet.s.sol         # One-shot testnet (token + oracle + vault)
├── DeployCrossChain.s.sol      # Router, adapter, satellite deploy
├── DeployNew.s.sol             # Phase 2 deploy (XCMExecutor, HyperExecutor, NativeAsset×2)
└── Demo.s.sol                  # Full lifecycle demo script
```

### Off-Chain Indexer (`obi.index`)

```
obi.index/
├── src/
│   ├── config/
│   │   ├── contracts.ts           # Canonical contract addresses (all deployed phases)
│   │   └── constants.ts           # Chain ID, RPC, poll interval, agent slippage
│   ├── graphql/
│   │   ├── typeDefs.ts            # GraphQL schema (queries + Subscription type)
│   │   ├── resolvers.ts           # Query + Subscription resolvers (graphql-ws)
│   │   └── pubsub.ts              # ObiPubSub — async iterator pub/sub (topic-keyed)
│   ├── sync/
│   │   ├── blockscout.ts          # Blockscout log poller (paginated, fromBlock cursor)
│   │   ├── decoder.ts             # ABI log decoder (ObidotVault, SwapRouter, KeeperOracle)
│   │   └── handlers/
│   │       ├── vault.ts           # Deposit, Withdraw, StrategyExecuted, StrategyOutcomeReported, IntentExecuted
│   │       ├── oracle.ts          # PriceUpdated, FeedSet → publishes ORACLE_UPDATED
│   │       └── router.ts          # Swapped → publishes SWAP_EXECUTED
│   ├── agent/
│   │   ├── orchestrator.ts        # Main loop: health check → evaluate → sign → execute
│   │   ├── strategy/
│   │   │   ├── evaluator.ts       # Vault state health check
│   │   │   └── arbitrage.ts       # Arbitrage opportunity detector
│   │   ├── intent/
│   │   │   ├── builder.ts         # UniversalIntent + StrategyIntent structs (mirrors IntentTypes.sol)
│   │   │   └── signer.ts          # EIP-712 sign (signUniversalIntent, signStrategyIntent)
│   │   ├── executor/
│   │   │   └── transaction.ts     # Submits signed intents on-chain via viem
│   │   └── llm/
│   │       ├── analyzer.ts        # LLM decision-making (OpenRouter / OpenAI / Anthropic)
│   │       └── provider.ts        # LLM provider factory
│   └── server.ts                  # Express + WebSocketServer + Apollo (port 4350)
├── prisma/schema.prisma           # PostgreSQL schema (Deposit, Withdrawal, StrategyExecution, …)
└── test/
    ├── pubsub.test.ts             # 12 tests — ObiPubSub delivery, isolation, cleanup
    ├── decoder.test.ts            # 8 tests — ABI log decoding
    ├── handlers.test.ts           # 9 tests — vault/oracle/router handlers (mock Prisma)
    └── blockscout.test.ts         # 5 tests — Blockscout poller (mock fetch)
```

### Dashboard App (`modules/app/`)

```
modules/app/src/
├── app/
│   ├── layout.tsx                 # Root layout (RainbowKit, TanStack Query)
│   ├── globals.css                # TailwindCSS v4 + design system utilities
│   ├── page.tsx                   # / Dashboard
│   ├── yields/page.tsx            # /yields
│   ├── strategies/page.tsx        # /strategies
│   ├── crosschain/page.tsx        # /crosschain
│   ├── agent/page.tsx             # /agent
│   └── api/[...path]/route.ts     # Proxy → GraphQL / obi.index
├── components/                    # All UI components per page
├── hooks/                         # useVaultData, useStrategies, useSubscription
└── lib/
    ├── api.ts                     # Typed GraphQL client (obi-kit)
    └── constants.ts               # Contract addresses, ABIs
```
