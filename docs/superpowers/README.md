# superpowers

This directory is archival planning material, not the current source of truth for the shipped product.

Use these files for historical context only. For live behavior, rely on:

- `README.md` in the repo root
- `docs/content/docs/*`
- `modules/agent/README.md`
- the current code under `modules/*`
